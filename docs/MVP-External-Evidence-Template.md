# MVP External Evidence Template

> Generated by `node scripts/generate_mvp_external_evidence_template.js`.
> Fill this template during external QA, then transfer sanitized evidence
> into the JSON ledgers. Do not commit raw screenshots or secrets.

## Summary

- Generated at: 2026-06-02
- Unresolved required items: 32
- Source ledgers:
  - `docs/MVP-Launch-Evidence.json`
  - `docs/Miniapp-Manual-QA.json`
  - `docs/Admin-Web-Manual-QA.json`

## Safety Rules

Do not record or commit:

- real AppID values
- openId/unionId
- auth tokens or cookies
- phone numbers or SMS codes
- passwords or private keys
- merchant credentials
- payment payloads
- raw screenshots with personal data
- full order/payment/refund identifiers

Record only sanitized aliases, masked suffixes, date/environment/result,
and a short operator note. Real payment, real refund, production data
mutation, security-group/firewall changes, `push main`, and
`workflow_dispatch` require explicit user approval first.

## Reusable Evidence Entry

Use this compact shape for any unresolved item, then copy the sanitized
summary into the matching JSON ledger:

```text
Evidence id:
Evidence date: YYYY-MM-DD
Environment: production | approved-staging | WeChat preview | real device | local devtools
Operator: <role label or reviewer initials>
Route/API: <route, page, or API alias>
Sanitized resource alias: <room/order/payment/refund/admin alias or id suffix only>
Result: passed | failed | waiver requested
Evidence summary: <short non-secret summary of what was observed>
Recovery or rollback: <restored final status, no mutation, or accepted change>
Follow-up: <remaining action or none>
```

For payment/refund, include an explicit approval note and low-value amount
band only. For deployment, include workflow run id/url, commit sha, target,
and smoke result. For security/firewall changes, include only sanitized rule
summaries.

## Launch Evidence

<!-- template:ADMIN-PROD-QA -->
- `ADMIN-PROD-QA`: Run node scripts/check_admin_web_manual_qa.js, execute pending checks with non-secret evidence, then require node scripts/check_admin_web_manual_qa.js --strict before marking this entry passed or waived.
<!-- template:CURRENT-BRANCH-DEPLOYED -->
- `CURRENT-BRANCH-DEPLOYED`: Ask for explicit approval before push/merge/workflow_dispatch, or record an explicit user decision that current-branch deployment evidence is out of scope.
<!-- template:MINIAPP-BOOKING-PATH -->
- `MINIAPP-BOOKING-PATH`: Run node scripts/check_miniapp_manual_qa.js, execute pending booking checks, then require node scripts/check_miniapp_manual_qa.js --strict before marking this entry passed or waived.
<!-- template:WECHAT-DOMAIN -->
- `WECHAT-DOMAIN`: Configure and verify HTTPS API domain, then record domain, verification date, and non-secret evidence summary in docs/Miniapp-Manual-QA.json.
<!-- template:WECHAT-PHONE -->
- `WECHAT-PHONE`: Run getPhoneNumber flow and record non-secret evidence in docs/Miniapp-Manual-QA.json.
<!-- template:WECHAT-PREVIEW-LOGIN -->
- `WECHAT-PREVIEW-LOGIN`: Run WeChat DevTools preview or real device with real AppID from ignored project.private.config.json and legal domain, then record pass/fail summary in docs/Miniapp-Manual-QA.json.
<!-- template:WECHAT-REAL-PAYMENT -->
- `WECHAT-REAL-PAYMENT`: After user approval, run a low-value payment and record only non-secret transaction evidence in docs/Miniapp-Manual-QA.json.
<!-- template:WECHAT-REAL-REFUND -->
- `WECHAT-REAL-REFUND`: After user approval, run refund validation and record only non-secret evidence in docs/Miniapp-Manual-QA.json.

## Miniapp Manual QA

<!-- template:MINIAPP-APPID-PREVIEW -->
- `MINIAPP-APPID-PREVIEW`: Copy project.private.config.example.json to ignored project.private.config.json, set the real AppID locally, run preview, and verify git diff does not include the real AppID.
<!-- template:MINIAPP-DOMAIN-HTTPS -->
- `MINIAPP-DOMAIN-HTTPS`: Set SUNFLOWER_API_BASE_URL to the HTTPS API domain in a local preview build and record domain/date/certificate result without secrets.
<!-- template:MINIAPP-ERROR-STATES -->
- `MINIAPP-ERROR-STATES`: Record sanitized examples from preview/real-device QA.
<!-- template:MINIAPP-HOME-CONTENT -->
- `MINIAPP-HOME-CONTENT`: Open home in preview/real device and record sanitized content-load result.
<!-- template:MINIAPP-MOCK-PAYMENT -->
- `MINIAPP-MOCK-PAYMENT`: Run in approved dev/test environment if mock mode is enabled and record sanitized result.
<!-- template:MINIAPP-ORDER-CREATE -->
- `MINIAPP-ORDER-CREATE`: Run with QA room/date and record sanitized order alias/id suffix only.
<!-- template:MINIAPP-ORDER-LIST-ACTIONS -->
- `MINIAPP-ORDER-LIST-ACTIONS`: Use QA orders and record sanitized action outcomes; waive unsafe production mutations only with explicit user approval.
<!-- template:MINIAPP-PHONE-BIND -->
- `MINIAPP-PHONE-BIND`: Run phone binding on preview/real device and record masked phone suffix or non-secret success summary only.
<!-- template:MINIAPP-REAL-PAYMENT -->
- `MINIAPP-REAL-PAYMENT`: Requires explicit user approval before real payment. Record non-secret transaction evidence only.
<!-- template:MINIAPP-REFUND -->
- `MINIAPP-REFUND`: Requires explicit user approval before real refund. Record non-secret result or waiver.
<!-- template:MINIAPP-ROOM-BROWSE -->
- `MINIAPP-ROOM-BROWSE`: Run booking/room-detail flow and record room alias/date range and sanitized result.
<!-- template:MINIAPP-WX-LOGIN -->
- `MINIAPP-WX-LOGIN`: Run login in develop/trial preview or real device and record sanitized result, envVersion, and token/profile success summary.

## Admin-Web Manual QA

<!-- template:ADMIN-AFTER-SALE -->
- `ADMIN-AFTER-SALE`: Run against QA after-sale/refund data or record explicit waiver with reason.
<!-- template:ADMIN-AUTH-ACTIVATE -->
- `ADMIN-AUTH-ACTIVATE`: Use allowlisted QA phone/account, then record masked phone suffix and sanitized success/failure summary only.
<!-- template:ADMIN-AUTH-LOGIN -->
- `ADMIN-AUTH-LOGIN`: Run with a dedicated non-production-impact admin account and record date, environment, account role label, and sanitized result.
<!-- template:ADMIN-AUTH-RESET-CHANGE -->
- `ADMIN-AUTH-RESET-CHANGE`: Run only on a dedicated QA account; restore final credentials outside Git.
<!-- template:ADMIN-ERROR-STATES -->
- `ADMIN-ERROR-STATES`: Record sanitized examples of redirect/error/empty/form feedback behavior.
<!-- template:ADMIN-ORDER-LIST-DETAIL -->
- `ADMIN-ORDER-LIST-DETAIL`: Record sanitized order id suffix or QA order alias, filter conditions, and result.
<!-- template:ADMIN-ORDER-OPS -->
- `ADMIN-ORDER-OPS`: Use only approved QA orders; otherwise record user waiver for unsafe production mutations.
<!-- template:ADMIN-PRICING-BATCH -->
- `ADMIN-PRICING-BATCH`: Use future QA dates to avoid live customer impact; record sanitized updated counts and restore, accept, or waive the final state explicitly.
<!-- template:ADMIN-PRICING-CALENDAR -->
- `ADMIN-PRICING-CALENDAR`: Record room alias, date range, and sanitized calendar load result.
<!-- template:ADMIN-ROOM-LIST-EDIT -->
- `ADMIN-ROOM-LIST-EDIT`: Use a clearly named QA room or an approved existing room; record before/after summary without raw customer data and restore or explicitly accept the final change.
<!-- template:ADMIN-ROOM-SHELF -->
- `ADMIN-ROOM-SHELF`: Run on QA room or approved room only; record the restored final status.
<!-- template:ADMIN-WORKSPACE-HEALTH -->
- `ADMIN-WORKSPACE-HEALTH`: Record sanitized browser/runtime result and API status summary.

## Final Checks

After transferring sanitized evidence into the JSON ledgers, run:

```bash
node scripts/check_mvp_launch_evidence.js
node scripts/check_miniapp_manual_qa.js
node scripts/check_admin_web_manual_qa.js
node scripts/check_mvp_closeout_readiness.js
```

Before declaring MVP complete, strict mode must pass:

```bash
node scripts/check_mvp_launch_evidence.js --strict
node scripts/check_miniapp_manual_qa.js --strict
node scripts/check_admin_web_manual_qa.js --strict
node scripts/check_mvp_closeout_readiness.js --strict
```

