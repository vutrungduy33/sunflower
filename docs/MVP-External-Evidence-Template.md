# MVP External Evidence Template

> Generated by `node scripts/generate_mvp_external_evidence_template.js`.
> Fill this template during external QA, then transfer sanitized evidence
> into the JSON ledgers. Do not commit raw screenshots or secrets.

## Summary

- Generated at: 2026-06-02
- Unresolved required items: 33
- Source ledgers:
  - `docs/MVP-Launch-Evidence.json`
  - `docs/Miniapp-Manual-QA.json`
  - `docs/Admin-Web-Manual-QA.json`

## Safety Rules

Do not record or commit:

- real AppID values
- openId/unionId
- auth tokens or cookies
- phone numbers or SMS codes
- passwords or private keys
- merchant credentials
- payment payloads
- raw screenshots with personal data
- full order/payment/refund identifiers

Record only sanitized aliases, masked suffixes, date/environment/result,
and a short operator note. Real payment, real refund, production data
mutation, security-group/firewall changes, `push main`, and
`workflow_dispatch` require explicit user approval first.

## Launch Evidence

<!-- template:ADMIN-PROD-QA -->
### ADMIN-PROD-QA

- Status now: `pending`
- Area: admin-web
- Requirement: A real admin account can complete activation/login/password/account, room/price/inventory, order operations, and summary checks against production or approved staging.
- Current evidence: Local automated admin tests passed. Dedicated admin manual QA ledger exists at docs/Admin-Web-Manual-QA.json, but production/staging manual QA with a real account is not recorded.
- Next action: Run node scripts/check_admin_web_manual_qa.js, execute pending checks with non-secret evidence, then require node scripts/check_admin_web_manual_qa.js --strict before marking this entry passed or waived.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:BACKEND-8080-HARDENING -->
### BACKEND-8080-HARDENING

- Status now: `pending`
- Area: security
- Requirement: Direct public access to backend port 8080 is restricted to ECS-1 by security group or host firewall, or explicitly accepted by the user as a launch risk.
- Current evidence: RUN_INTERNAL=1 scripts/check_backend_8080_exposure.sh passed read-only checks: public 8080 was not directly usable from this network, ECS-1 private upstream works, and ECS-2 backend is healthy. ECS-2 still shows docker-proxy listening on 0.0.0.0:8080, and local firewall output did not prove restriction.
- Next action: Record Alibaba Cloud security group evidence in docs/Backend-8080-Security.md or get explicit user waiver before marking this entry passed or waived. Modifying security group/firewall requires user approval.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Security rule summary: `<security group/firewall rule summary, no console secrets>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:CURRENT-BRANCH-DEPLOYED -->
### CURRENT-BRANCH-DEPLOYED

- Status now: `pending`
- Area: deployment
- Requirement: Current committed MVP branch code has been deployed through the approved GitHub Actions path, or deployment is explicitly declared out of scope by the user.
- Current evidence: Current branch codex/s18-payment-hardening has not been pushed or merged to main; no deployment was triggered by the latest MVP commits.
- Next action: Ask for approval before push/merge/workflow_dispatch.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Workflow evidence: `<workflow run id/url, commit sha, target, smoke result>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-BOOKING-PATH -->
### MINIAPP-BOOKING-PATH

- Status now: `pending`
- Area: miniapp
- Requirement: User can browse home/rooms/calendar, create an order, and see it in the order center in preview or real-device flow.
- Current evidence: Automated smoke covers file registration and syntax only. Dedicated miniapp manual QA ledger exists at docs/Miniapp-Manual-QA.json, but user-path manual evidence is not recorded.
- Next action: Run node scripts/check_miniapp_manual_qa.js, execute pending booking checks, then require node scripts/check_miniapp_manual_qa.js --strict before marking this entry passed or waived.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:WECHAT-DOMAIN -->
### WECHAT-DOMAIN

- Status now: `pending`
- Area: miniapp
- Requirement: Miniapp production API uses an HTTPS legal request domain configured in the WeChat backend, with valid certificate and ICP/domain eligibility.
- Current evidence: Not recorded in repo. Current default API base remains bare HTTP IP for DevTools validation only. Detailed miniapp QA ledger exists at docs/Miniapp-Manual-QA.json.
- Next action: Configure and verify HTTPS API domain, then record domain, verification date, and non-secret evidence summary in docs/Miniapp-Manual-QA.json.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:WECHAT-PHONE -->
### WECHAT-PHONE

- Status now: `pending`
- Area: miniapp
- Requirement: Phone authorization and backend phone binding succeed in preview or real-device flow.
- Current evidence: Not recorded in repo. Detailed miniapp QA ledger exists at docs/Miniapp-Manual-QA.json.
- Next action: Run getPhoneNumber flow and record non-secret evidence in docs/Miniapp-Manual-QA.json.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:WECHAT-PREVIEW-LOGIN -->
### WECHAT-PREVIEW-LOGIN

- Status now: `pending`
- Area: miniapp
- Requirement: WeChat preview or real-device login obtains backend token and stores profile state without devtools-only bypasses.
- Current evidence: Not recorded in repo. Detailed miniapp QA ledger exists at docs/Miniapp-Manual-QA.json.
- Next action: Run WeChat DevTools preview or real device with real AppID and legal domain, then record pass/fail summary in docs/Miniapp-Manual-QA.json.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:WECHAT-REAL-PAYMENT -->
### WECHAT-REAL-PAYMENT

- Status now: `pending`
- Area: payment
- Requirement: One low-value real WeChat payment completes with merchant configuration, backend confirmation, and order status update.
- Current evidence: Not recorded in repo. Detailed miniapp/payment QA ledger exists at docs/Miniapp-Manual-QA.json.
- Next action: After user approval, run a low-value payment and record only non-secret transaction evidence in docs/Miniapp-Manual-QA.json.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Approval note: `<explicit user approval before real payment/refund>`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Amount band: `<low-value / test amount band only>`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:WECHAT-REAL-REFUND -->
### WECHAT-REAL-REFUND

- Status now: `pending`
- Area: payment
- Requirement: One real refund or refund request path is validated with merchant configuration and backend status update.
- Current evidence: Not recorded in repo. Detailed miniapp/payment QA ledger exists at docs/Miniapp-Manual-QA.json.
- Next action: After user approval, run refund validation and record only non-secret evidence in docs/Miniapp-Manual-QA.json.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Approval note: `<explicit user approval before real payment/refund>`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Amount band: `<low-value / test amount band only>`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

## Miniapp Manual QA

<!-- template:MINIAPP-APPID-PREVIEW -->
### MINIAPP-APPID-PREVIEW

- Status now: `pending`
- Area: environment
- Route: project.config.json
- Requirement: Real preview/real-device run uses a local uncommitted real AppID while the committed project.config.json remains touristappid.
- Related APIs/capabilities: `wx.login`
- Current evidence: Not recorded in repo.
- Next action: Run preview with local AppID and verify git diff does not include the real AppID.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-DOMAIN-HTTPS -->
### MINIAPP-DOMAIN-HTTPS

- Status now: `pending`
- Area: domain
- Route: global api base
- Requirement: Preview/release API requests use an HTTPS domain that is configured as a legal WeChat request domain and has a valid certificate.
- Related APIs/capabilities: `/api/health`, `/api/content/home`
- Current evidence: Not recorded in repo. Default API base remains bare HTTP IP for DevTools/local validation only.
- Next action: Set SUNFLOWER_API_BASE_URL to the HTTPS API domain in a local preview build and record domain/date/certificate result without secrets.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-ERROR-STATES -->
### MINIAPP-ERROR-STATES

- Status now: `pending`
- Area: resilience
- Route: pages/mvp/login/index pages/mvp/order-create/index pages/mvp/order-list/index
- Requirement: Expired token, network/API failure, payment cancel/failure, invalid form data, and denied phone authorization show understandable feedback and recovery path.
- Related APIs/capabilities: `/api/users/me`, `/api/orders`, `wx.requestPayment`, `getPhoneNumber`
- Current evidence: Not recorded in repo.
- Next action: Record sanitized examples from preview/real-device QA.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-HOME-CONTENT -->
### MINIAPP-HOME-CONTENT

- Status now: `pending`
- Area: browse
- Route: pages/mvp/home/index
- Requirement: Home loads banners, services, featured rooms, member benefits, and uses the configured API base.
- Related APIs/capabilities: `/api/content/home`
- Current evidence: Automated smoke covers files/syntax only; preview or real-device content load is not recorded.
- Next action: Open home in preview/real device and record sanitized content-load result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-MOCK-PAYMENT -->
### MINIAPP-MOCK-PAYMENT

- Status now: `pending`
- Area: payment
- Route: pages/mvp/order-create/index pages/mvp/order-list/index
- Requirement: When backend explicitly returns MOCK_WECHAT_PAY, payment flow confirms through /api/orders/{orderId}/pay/confirm and order list refreshes.
- Related APIs/capabilities: `/api/orders/{orderId}/pay`, `/api/orders/{orderId}/pay/confirm`
- Current evidence: Code path exists in utils/mvp/payment.js, but preview/manual result is not recorded.
- Next action: Run in approved dev/test environment if mock mode is enabled and record sanitized result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Approval note: `<explicit user approval before real payment/refund>`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Amount band: `<low-value / test amount band only>`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-ORDER-CREATE -->
### MINIAPP-ORDER-CREATE

- Status now: `pending`
- Area: booking
- Route: pages/mvp/order-create/index
- Requirement: Order create blocks invalid/unbound-phone submission, accepts valid guest data after phone binding, creates an order, and navigates to pay or order list.
- Related APIs/capabilities: `/api/orders`
- Current evidence: Not recorded in repo.
- Next action: Run with QA room/date and record sanitized order alias/id suffix only.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-ORDER-LIST-ACTIONS -->
### MINIAPP-ORDER-LIST-ACTIONS

- Status now: `pending`
- Area: orders
- Route: pages/mvp/order-list/index
- Requirement: Order list loads, filters supported statuses, can cancel an unpaid order, submit reschedule request for eligible order, and submit refund request for eligible order.
- Related APIs/capabilities: `/api/orders`, `/api/orders/{orderId}/cancel`, `/api/orders/{orderId}/reschedule`, `/api/orders/{orderId}/refund`
- Current evidence: Not recorded in repo.
- Next action: Use QA orders and record sanitized action outcomes; waive unsafe production mutations only with explicit user approval.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-PHONE-BIND -->
### MINIAPP-PHONE-BIND

- Status now: `pending`
- Area: auth
- Route: pages/mvp/order-create/index
- Requirement: getPhoneNumber returns a phone authorization code, /api/auth/bind-phone succeeds, and the order form receives the bound phone.
- Related APIs/capabilities: `getPhoneNumber`, `/api/auth/bind-phone`
- Current evidence: Not recorded in repo.
- Next action: Run phone binding on preview/real device and record masked phone suffix or non-secret success summary only.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-REAL-PAYMENT -->
### MINIAPP-REAL-PAYMENT

- Status now: `pending`
- Area: payment
- Route: pages/mvp/order-create/index pages/mvp/order-list/index
- Requirement: In production payment mode, wx.requestPayment is invoked, one low-value payment succeeds, backend confirm/status update is visible, and cancel/failure feedback is understandable.
- Related APIs/capabilities: `wx.requestPayment`, `/api/orders/{orderId}/pay`, `/api/orders/{orderId}/pay/confirm`
- Current evidence: Not recorded in repo.
- Next action: Requires explicit user approval before real payment. Record non-secret transaction evidence only.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Approval note: `<explicit user approval before real payment/refund>`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Amount band: `<low-value / test amount band only>`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-REFUND -->
### MINIAPP-REFUND

- Status now: `pending`
- Area: payment
- Route: pages/mvp/order-list/index
- Requirement: Refund request or real refund path is validated with merchant/backend status update, or explicitly waived when no safe payment/refund data exists.
- Related APIs/capabilities: `/api/orders/{orderId}/refund`, `/api/payments/wechat/refunds/notify`
- Current evidence: Not recorded in repo.
- Next action: Requires explicit user approval before real refund. Record non-secret result or waiver.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Approval note: `<explicit user approval before real payment/refund>`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Amount band: `<low-value / test amount band only>`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-ROOM-BROWSE -->
### MINIAPP-ROOM-BROWSE

- Status now: `pending`
- Area: browse
- Route: pages/mvp/booking/index pages/mvp/room-detail/index
- Requirement: User can browse rooms, filter by keyword/date, open room detail, view calendar/pricing/rules, and navigate to order creation.
- Related APIs/capabilities: `/api/rooms`, `/api/rooms/{roomId}`, `/api/rooms/{roomId}/calendar`
- Current evidence: Not recorded in repo.
- Next action: Run booking/room-detail flow and record room alias/date range and sanitized result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:MINIAPP-WX-LOGIN -->
### MINIAPP-WX-LOGIN

- Status now: `pending`
- Area: auth
- Route: pages/mvp/login/index
- Requirement: wx.login obtains a code, backend /api/auth/wechat/login returns token/profile, token is stored, and /api/users/me can restore profile state.
- Related APIs/capabilities: `wx.login`, `/api/auth/wechat/login`, `/api/users/me`
- Current evidence: Not recorded in repo.
- Next action: Run login in develop/trial preview or real device and record sanitized result, envVersion, and token/profile success summary.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

## Admin-Web Manual QA

<!-- template:ADMIN-AFTER-SALE -->
### ADMIN-AFTER-SALE

- Status now: `pending`
- Area: orders
- Route: /orders
- Requirement: After-sale approve/reject and failed refund retry controls are validated when matching QA data exists, or explicitly waived if no safe data exists.
- Related APIs/capabilities: `/api/admin/orders/{orderId}/after-sale/{requestId}/approve`, `/api/admin/orders/{orderId}/after-sale/{requestId}/reject`, `/api/admin/orders/{orderId}/refunds/{refundId}/retry`
- Current evidence: Not recorded in repo.
- Next action: Run against QA after-sale/refund data or record explicit waiver with reason.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-AUTH-ACTIVATE -->
### ADMIN-AUTH-ACTIVATE

- Status now: `pending`
- Area: auth
- Route: /activate
- Requirement: Allowlisted admin account can request SMS code and complete first activation without committing the phone number or SMS code.
- Related APIs/capabilities: `/api/admin/auth/sms-code`, `/api/admin/auth/activate`
- Current evidence: Not recorded in repo.
- Next action: Use allowlisted QA phone/account, then record masked phone suffix and sanitized success/failure summary only.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-AUTH-LOGIN -->
### ADMIN-AUTH-LOGIN

- Status now: `pending`
- Area: auth
- Route: /login
- Requirement: Dedicated QA admin can login with password, reach the protected workspace, refresh the browser, recover the session through /api/admin/account/me, and logout cleanly.
- Related APIs/capabilities: `/api/admin/auth/login`, `/api/admin/account/me`, `/api/admin/auth/logout`
- Current evidence: Not recorded in repo.
- Next action: Run with a dedicated non-production-impact admin account and record date, environment, account role label, and sanitized result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-AUTH-RESET-CHANGE -->
### ADMIN-AUTH-RESET-CHANGE

- Status now: `pending`
- Area: auth
- Route: /reset-password /account/password
- Requirement: Password reset and in-session password change work for a dedicated QA account, and the old session/password cannot continue unintentionally.
- Related APIs/capabilities: `/api/admin/auth/reset-password`, `/api/admin/account/change-password`
- Current evidence: Not recorded in repo.
- Next action: Run only on a dedicated QA account; restore final credentials outside Git.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-ERROR-STATES -->
### ADMIN-ERROR-STATES

- Status now: `pending`
- Area: resilience
- Route: /login /rooms /pricing /orders
- Requirement: Unauthorized session, backend error, empty state, and invalid form feedback are understandable and do not trap the operator.
- Related APIs/capabilities: `/api/admin/account/me`, `/api/admin/rooms`, `/api/admin/orders`
- Current evidence: Not recorded in repo.
- Next action: Record sanitized examples of redirect/error/empty/form feedback behavior.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-ORDER-LIST-DETAIL -->
### ADMIN-ORDER-LIST-DETAIL

- Status now: `pending`
- Area: orders
- Route: /orders
- Requirement: Order list, status filter, keyword filter, check-in date range filter, and detail drawer work against current backend data.
- Related APIs/capabilities: `/api/admin/orders`, `/api/admin/orders/{orderId}`
- Current evidence: Not recorded in repo.
- Next action: Record sanitized order id suffix or QA order alias, filter conditions, and result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-ORDER-OPS -->
### ADMIN-ORDER-OPS

- Status now: `pending`
- Area: orders
- Route: /orders
- Requirement: Check-in, check-out, no-show, reschedule, and direct refund actions are validated with approved QA orders or explicitly waived when no safe data exists.
- Related APIs/capabilities: `/api/admin/orders/{orderId}/check-in`, `/api/admin/orders/{orderId}/check-out`, `/api/admin/orders/{orderId}/no-show`, `/api/admin/orders/{orderId}/reschedule`, `/api/admin/orders/{orderId}/refund`
- Current evidence: Not recorded in repo.
- Next action: Use only approved QA orders; otherwise record user waiver for unsafe production mutations.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-PRICING-BATCH -->
### ADMIN-PRICING-BATCH

- Status now: `pending`
- Area: pricing
- Route: /pricing
- Requirement: Batch price and inventory update succeeds for approved QA dates and can be restored or accepted as intended.
- Related APIs/capabilities: `/api/admin/room-prices`, `/api/admin/room-inventory`
- Current evidence: Not recorded in repo.
- Next action: Use future QA dates to avoid live customer impact; record sanitized updated counts and restoration decision.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-PRICING-CALENDAR -->
### ADMIN-PRICING-CALENDAR

- Status now: `pending`
- Area: pricing
- Route: /pricing
- Requirement: Pricing page loads rooms and calendar for an approved room/date range.
- Related APIs/capabilities: `/api/admin/rooms`, `/api/rooms/{roomId}/calendar`
- Current evidence: Not recorded in repo.
- Next action: Record room alias, date range, and sanitized calendar load result.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-ROOM-LIST-EDIT -->
### ADMIN-ROOM-LIST-EDIT

- Status now: `pending`
- Area: rooms
- Route: /rooms
- Requirement: Room list loads, keyword/status filtering works, and a test room can be created or edited with reversible field changes.
- Related APIs/capabilities: `/api/admin/rooms`, `/api/admin/rooms/{roomId}`
- Current evidence: Not recorded in repo.
- Next action: Use a clearly named QA room or an approved existing room; record before/after summary without raw customer data.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-ROOM-SHELF -->
### ADMIN-ROOM-SHELF

- Status now: `pending`
- Area: rooms
- Route: /rooms
- Requirement: Room shelf status can be toggled active/inactive and restored to the original state.
- Related APIs/capabilities: `/api/admin/rooms/{roomId}`
- Current evidence: Not recorded in repo.
- Next action: Run on QA room or approved room only; record the restored final status.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

<!-- template:ADMIN-WORKSPACE-HEALTH -->
### ADMIN-WORKSPACE-HEALTH

- Status now: `pending`
- Area: workspace
- Route: /
- Requirement: Workspace loads protected shell, account header, business overview cards, backend health, and report summary without console/runtime errors.
- Related APIs/capabilities: `/api/health`, `/api/admin/reports/summary`
- Current evidence: Not recorded in repo.
- Next action: Record sanitized browser/runtime result and API status summary.

Suggested sanitized evidence entry:

- Evidence date: `YYYY-MM-DD`
- Environment: `production | approved-staging | WeChat preview | real device | local devtools`
- Operator: `<role label or reviewer initials>`
- Route/API: `<route, page, or API alias>`
- Sanitized resource alias: `<room/order/payment/refund/admin alias or id suffix only>`
- Result: `passed | failed | waived request`
- Evidence summary: `<short non-secret summary of what was observed>`
- Recovery or rollback: `<restored final status, no mutation, or accepted change>`
- Follow-up: `<remaining action or none>`

Ledger update reminder:

- Change `status` only after the evidence above is actually available.
- Use `waived` only with explicit user acceptance and reason.
- Keep `evidence` at least one compact sentence with non-secret proof.

## Final Checks

After transferring sanitized evidence into the JSON ledgers, run:

```bash
node scripts/check_mvp_launch_evidence.js
node scripts/check_miniapp_manual_qa.js
node scripts/check_admin_web_manual_qa.js
node scripts/check_mvp_closeout_readiness.js
```

Before declaring MVP complete, strict mode must pass:

```bash
node scripts/check_mvp_launch_evidence.js --strict
node scripts/check_miniapp_manual_qa.js --strict
node scripts/check_admin_web_manual_qa.js --strict
node scripts/check_mvp_closeout_readiness.js --strict
```

