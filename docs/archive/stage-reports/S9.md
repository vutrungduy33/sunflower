# S9 Stage Report

## 需求确认

- 本 Stage 目标：创建 `sunflower-admin-web` 工程并完成 `React 18 + TypeScript + Vite + TDesign React` 基线，接入路由、HTTP 客户端、环境配置、开发代理与 `lint/test/build` 脚本。
- 本 Stage 边界（不做）：不进入 S10 登录与权限、不实现房型/价格/订单业务页面、不变更后端 API 契约。

## 代码改动

- 主要改动文件：
  - `sunflower-admin-web/.nvmrc`
  - `sunflower-admin-web/.env.development`
  - `sunflower-admin-web/package.json`
  - `sunflower-admin-web/package-lock.json`
  - `sunflower-admin-web/README.md`
  - `sunflower-admin-web/vite.config.ts`
  - `sunflower-admin-web/tsconfig.app.json`
  - `sunflower-admin-web/src/App.tsx`
  - `sunflower-admin-web/src/main.tsx`
  - `sunflower-admin-web/src/app/router.tsx`
  - `sunflower-admin-web/src/app/shell-layout.tsx`
  - `sunflower-admin-web/src/config/env.ts`
  - `sunflower-admin-web/src/pages/workspace-page.tsx`
  - `sunflower-admin-web/src/pages/foundations-page.tsx`
  - `sunflower-admin-web/src/pages/not-found-page.tsx`
  - `sunflower-admin-web/src/services/http.ts`
  - `sunflower-admin-web/src/services/health.ts`
  - `sunflower-admin-web/src/setupTests.ts`
  - `sunflower-admin-web/src/styles/index.css`
  - `sunflower-admin-web/src/test/app.test.tsx`
  - `docs/Web-Admin-Plan.md`
  - `docs/Backlog.md`
  - `docs/README.md`
  - `README.md`
- 关键实现点：
  - 基于 Vite `react-ts` 模板初始化 `sunflower-admin-web`，并固定 `Node.js >= 20.19.0`、`npm >= 10` 约束。
  - 接入 `TDesign React`、`React Router`、`TanStack Query`、`Axios`，建立工作台、工程基线页和 404 占位页。
  - 建立 `src/config/env.ts` 与 Vite `/api` 代理配置，统一环境变量与 HTTP 基线。
  - 建立 `Vitest + Testing Library` 测试基线，补充健康检查 mock 冒烟测试。
  - 在 `$HOME/.local/node-v20.20.1-darwin-arm64` 安装 Node `v20.20.1`，并通过 `.nvmrc` 记录项目推荐版本。

## 自动化测试

- 执行命令：`./scripts/stage_guard.sh pre S9`
- 结果：通过。
- 执行命令：`make stage-pre STAGE=S9`
- 结果：通过。
- 执行命令：`env PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH" npm run test`
- 结果：通过（`1` 个测试文件，`1` 条测试用例全部通过）。
- 执行命令：`env PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH" npm run lint`
- 结果：通过。
- 执行命令：`env PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH" npm run build`
- 结果：通过（Vite 生产构建成功）。
- 执行命令：`env PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH" npm run dev -- --host 127.0.0.1 --port 4173`
- 结果：通过（本地开发服务器可启动，随后使用 `curl -I http://127.0.0.1:4173` 验证返回 `HTTP/1.1 200 OK`）。
- 执行命令：`make stage-post STAGE=S9`
- 结果：通过。
- 执行命令：`./scripts/stage_guard.sh post S9`
- 结果：通过。

## 人工复核步骤

1. 执行 `export PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH"`，然后运行 `node -v`，确认输出为 `v20.20.1`。
2. 在仓库根目录启动后端：`docker compose up -d mysql backend`，再执行 `curl http://localhost:8080/api/health`，确认健康检查可用。
3. 进入 `sunflower-admin-web/` 执行 `npm run dev`，打开浏览器访问 `http://localhost:5173/`，确认工作台可打开且顶部显示 `S9 基线完成`。
4. 在页面中确认存在“后端连通性”和“当前已接入能力”卡片；当后端可用时，健康状态显示 `UP`，否则显示不可达提示。
5. 访问 `http://localhost:5173/foundations`，确认工程基线页展示目录基线和工程约束。

## DoD Checklist

- [x] 范围单一（仅本 Stage）
- [x] 自动化测试已执行并通过
- [x] 人工复核步骤可复现
- [x] Backlog 状态已更新
- [x] `make stage-pre/post` 已通过

## API 契约影响

- 是否变更 API 契约：否

## 风险与后续

- 当前已知风险：
  - 当前系统默认 Node 版本仍为 `v18.4.0`，若未通过 `PATH` 或 `nvm use` 切换到 `20.20.1`，`sunflower-admin-web` 的开发命令仍会失败。
  - 当前工作台仅做健康检查和工程能力展示，实际业务页仍待 S10-S13 分阶段接入。
- 下一 Stage 建议：
  - 进入 `S10`，补齐登录页、鉴权守卫、基础布局和菜单权限骨架。
