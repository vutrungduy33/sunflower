# S14 Stage Report

## 需求确认

- 本 Stage 目标：补齐后端与 Web 管理端的 CI/CD，完成 ECS 自动部署收口，并形成可复现的发布与回滚基线。
- 本 Stage 边界（不做）：不实现 S10-S13 页面功能；不改动后端 API 契约；不引入 Kubernetes、服务网格或额外云原生发布平台。

## 代码改动

- 主要改动文件：
  - `sunflower-backend/.mvn/settings-docker.xml`
  - `sunflower-backend/Dockerfile`
  - `.github/workflows/deploy-backend.yml`
  - `.github/workflows/pr-stage-gate.yml`
  - `deploy/nginx/sunflower-host.conf.template`
  - `docker-compose.yml`
  - `scripts/start_backend_with_mvp_seed.sh`
  - `scripts/start_admin_web.sh`
  - `scripts/reload_host_nginx.sh`
  - `sunflower-admin-web/.dockerignore`
  - `sunflower-admin-web/Dockerfile`
  - `sunflower-admin-web/nginx.conf`
  - `sunflower-miniapp/README.md`
  - `sunflower-miniapp/app.js`
  - `sunflower-miniapp/utils/mvp/api.js`
  - `docs/Miniapp-Frontend-MVP.md`
  - `docs/CI-CD.md`
  - `docs/Backlog.md`
  - `docs/stage-reports/S14.md`
- 关键实现点：
  - `Deploy Services To ECS` 工作流按 push 变更自动识别 backend、admin-web、入口配置三类发布目标，并分别构建 GHCR 镜像。
  - backend 镜像构建阶段默认使用 `sunflower-backend/.mvn/settings-docker.xml` 指向阿里云 Maven 公共镜像，并先执行 `dependency:go-offline` 预热依赖层，减少冷启动下载耗时。
  - deploy workflow 在 Runner 打包 deployment bundle 并上传到 ECS，再由 ECS 本地解压执行，避免服务器侧 `git clone/fetch` 受 GitHub TLS 或出网波动影响。
  - ECS 部署顺序固定为“backend -> admin-web -> host nginx”，先切换后端，再切换前端，最后刷新宿主机入口，避免用户流量进入未就绪服务。
  - `scripts/start_admin_web.sh` 在启动前强制等待 backend 健康，并在 admin-only 部署时改为执行 `docker compose up -d --no-deps admin-web`，避免 Compose 触碰 backend 服务并回退到 `sunflower-backend:local` 本地构建。
  - `scripts/reload_host_nginx.sh` 会在刷新宿主机 Nginx 前再次确认 backend 与 admin-web 都已健康。
  - `docker-compose.yml` 收敛为 `mysql`、`backend`、`admin-web` 三个容器，全部只绑定 ECS 回环地址，不再创建容器化 `edge-gateway`。
  - `deploy/nginx/sunflower-host.conf.template` 把 `/` 转发到 `127.0.0.1:${ADMIN_WEB_HOST_PORT:-18080}`，把 `/api/` 转发到 `127.0.0.1:${BACKEND_HOST_PORT:-8080}`，统一 Web 管理端与小程序的后端入口语义。
  - `sunflower-admin-web/nginx.conf` 收缩为纯静态站点服务，不再承担 API 反代职责。
  - `sunflower-backend/Dockerfile` 安装 `curl`，并把 backend healthcheck 统一为 `curl` 检查，避免镜像内缺少 `wget` 导致误判不健康。
  - `sunflower-miniapp/utils/mvp/api.js` 的默认 API 基地址与 `app.js` 对齐，避免 `http/https` 默认值不一致。
  - `pr-stage-gate.yml` 补齐 Node 20 环境和 Web 的 `npm ci/lint/test/build`，把 Web 纳入 CI 门禁。
  - 2026-03-12 deploy follow-up：`Deploy via SSH` 补齐可选的 `ECS_SSH_PASSPHRASE` 输入，并在文档中明确 SSH 握手失败时优先检查私钥/口令与 ECS `authorized_keys`。
  - 2026-03-12 deploy reliability hotfix：移除 ECS 上的 `git fetch origin main` 依赖，改为 Actions 侧上传 deployment bundle，避免 `GnuTLS recv error` 类仓库拉取失败阻塞发布。
  - 2026-03-12 ingress consolidation hotfix：基于线上排查结果，发现宿主机 `nginx.service` 已占用 `0.0.0.0:80`，因此收敛为“宿主机 Nginx 唯一公网入口”，移除容器 `edge-gateway`，避免重复抢占 80 端口。

## 自动化测试

- 执行命令：`ruby -e "require 'yaml'; YAML.load_file('.github/workflows/deploy-backend.yml'); YAML.load_file('.github/workflows/pr-stage-gate.yml')"`
- 结果：通过（两个 GitHub Actions workflow 都能被 YAML 解析）。
- 执行命令：`tar -czf /tmp/sunflower-deploy-bundle.tgz docker-compose.yml deploy/nginx/sunflower-host.conf.template scripts/start_backend_with_mvp_seed.sh scripts/start_admin_web.sh scripts/reload_host_nginx.sh scripts/sql/mvp_demo_seed.sql && tar -tzf /tmp/sunflower-deploy-bundle.tgz`
- 结果：通过（deployment bundle 可成功打包并包含 compose、宿主机 Nginx 模板、部署脚本与 seed SQL）。
- 执行命令：`bash -n scripts/start_backend_with_mvp_seed.sh`
- 结果：通过（脚本语法校验通过）。
- 执行命令：`bash -n scripts/start_admin_web.sh`
- 结果：通过（脚本语法校验通过）。
- 执行命令：`bash -n scripts/reload_host_nginx.sh`
- 结果：通过（脚本语法校验通过）。
- 执行命令：`tmp_bin=$(mktemp -d) && log_file=$(mktemp) && cat >"$tmp_bin/docker" <<'EOF'\n#!/usr/bin/env bash\nset -e\nprintf 'docker %s\\n' \"$*\" >> \"$LOG_FILE\"\nif [ \"$1\" = compose ] && [ \"$2\" = version ]; then\n  exit 0\nfi\nexit 0\nEOF\ncat >"$tmp_bin/curl" <<'EOF'\n#!/usr/bin/env bash\nexit 0\nEOF\nchmod +x \"$tmp_bin/docker\" \"$tmp_bin/curl\" && LOG_FILE=\"$log_file\" PATH=\"$tmp_bin:$PATH\" ADMIN_WEB_IMAGE=ghcr.io/example/sunflower-admin-web:test BACKEND_HOST_PORT=8080 ADMIN_WEB_HOST_PORT=18080 bash scripts/start_admin_web.sh && grep -F \"docker compose up -d --no-deps admin-web\" \"$log_file\"`
- 结果：通过（admin-only 启动路径只执行 `pull admin-web` 与 `up -d --no-deps admin-web`，未触发 backend 依赖启动）。
- 执行命令：`ruby -e "require 'yaml'; compose = YAML.load_file('docker-compose.yml'); services = compose.fetch('services'); abort('edge-gateway still present') if services.key?('edge-gateway'); abort('backend missing') unless services.key?('backend'); abort('admin-web missing') unless services.key?('admin-web'); abort('mysql missing') unless services.key?('mysql')"`
- 结果：通过（部署拓扑校验成功，`docker-compose.yml` 仅保留 `mysql`、`backend`、`admin-web` 三个服务，不再创建 `edge-gateway`）。
- 执行命令：`AUTH_TOKEN_SECRET=dummy BACKEND_IMAGE=ghcr.io/example/sunflower-backend:test docker compose config >/tmp/sunflower.compose.rendered.yml`
- 结果：通过（compose 渲染成功，输出 109 行配置）。
- 执行命令：`cd sunflower-backend && mvn -B test -Dtest=WechatCode2SessionClientTests`
- 结果：通过（`Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`）。
- 执行命令：`cd sunflower-backend && tmp_repo=$(mktemp -d) && mvn -B -s .mvn/settings-docker.xml -Dmaven.repo.local="$tmp_repo" dependency:get -Dartifact=org.assertj:assertj-core:3.22.0 -Dtransitive=false && rm -rf "$tmp_repo"`
- 结果：通过（全新临时仓库拉取成功，日志显示依赖从 `aliyun-public` 下载，`BUILD SUCCESS`，总耗时 22.115 秒）。
- 执行命令：`docker build --progress=plain -t sunflower-backend:test ./sunflower-backend`
- 结果：当前执行环境未连接 Docker daemon，返回 `Cannot connect to the Docker daemon at unix:///var/run/docker.sock`，因此未能在本机补跑镜像级验证。
- 执行命令：`./scripts/stage_guard.sh post S14`
- 结果：通过（含 API contract guard 与 Stage 报告结构校验）。

## 人工复核步骤

1. 在 GitHub Secrets 中确认 `ECS_HOST`、`ECS_USER`、`ECS_PORT`、`ECS_SSH_KEY`、`DEPLOY_PATH`、`GHCR_USERNAME`、`GHCR_TOKEN`、`AUTH_TOKEN_SECRET` 已配置；若私钥带口令，再确认 `ECS_SSH_PASSPHRASE`。
2. 在 GitHub Variables 中按需配置 `BACKEND_HOST_PORT`、`ADMIN_WEB_HOST_PORT`、`MYSQL_HOST_PORT`；未配置时分别默认使用 `8080`、`18080`、`3306`。
3. 向 `main` 推送一条同时命中 `sunflower-backend/**` 和 `sunflower-admin-web/**` 的提交，触发 `.github/workflows/deploy-backend.yml`。
4. 在 Actions 日志确认 workflow 先完成 backend 镜像构建与 `scripts/start_backend_with_mvp_seed.sh`，随后执行 `scripts/start_admin_web.sh`，最后执行 `scripts/reload_host_nginx.sh`。
5. 再向 `main` 推送一条只命中 `sunflower-admin-web/**` 的提交，确认 deploy 日志中出现 `deploying admin web second`，且 `scripts/start_admin_web.sh` 只输出 admin-web 的 `pull`/`up --no-deps` 路径，没有出现 `Starting backend service with local build`。
6. 在 ECS 上执行 `docker ps --format '{{.Names}} {{.Ports}}' | grep 'sunflower-'`，确认只存在 `sunflower-backend`、`sunflower-admin-web`、`sunflower-mysql` 三个容器，且三者均只绑定 `127.0.0.1` 回环端口。
7. 在 ECS 上执行 `sudo nginx -T | grep -n '127.0.0.1:18080\\|127.0.0.1:8080'`，确认宿主机 Nginx 把 `/` 转发到 admin-web、把 `/api/` 转发到 backend。
8. 访问 `http://<ecs-host>/healthz` 确认返回 `ok`，再打开 `http://<ecs-host>/`，确认管理后台可用且页面请求 `/api/health` 成功。
9. 在小程序中将 `SUNFLOWER_API_BASE_URL` 或 `app.globalData.apiBaseUrl` 设为同一 ECS 的统一入口地址，确认 `/api/*` 请求可正常返回。

## DoD Checklist

- [x] 范围单一（仅本 Stage）
- [x] 自动化测试已执行并通过
- [x] 人工复核步骤可复现
- [x] Backlog 状态已更新
- [x] `make stage-pre/post` 已通过

## API 契约影响

- 是否变更 API 契约：否

## 风险与后续

- 当前已知风险：
  - 当前统一入口已完成同机路径转发，但仓库内尚未落 443/TLS；若要给小程序正式环境使用，仍需补齐域名与 HTTPS 终止。
  - 本地 Docker daemon 当前不可用，无法完成镜像级 `docker build` 冒烟；本次以 workflow YAML 解析、部署拓扑 YAML 校验、脚本行为校验和 Maven/Compose 回归作为替代校验。
  - 宿主机 Nginx 配置现在由 deploy workflow 覆盖，部署用户必须具备 `root` 或 `sudo` 权限；若后续 ECS 的系统服务策略收紧，需要同步调整发布权限。
  - `GHCR_TOKEN` 若过期或权限回收，会导致 ECS 拉镜像失败；需纳入凭据轮换与告警策略。
  - 阿里云 Maven 公共镜像可用性若波动，会影响 Docker 构建阶段；必要时可在后续将镜像地址改为 workflow build-arg，以便按环境切换。
  - admin-only 部署现在依赖现有 backend 已经健康；如果服务器上 backend 被手工删掉或已失效，前端部署会按设计直接失败，需要先补一次 backend 发布。
- 下一 Stage 建议：
  - 回到主线继续执行 `S11`，补齐管理后台房型管理页面。
