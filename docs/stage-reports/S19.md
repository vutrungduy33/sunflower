# S19 Stage Report

## 需求确认

- 本 Stage 目标：清理正式上线前的部署与域名阻塞项，统一 prod 配置归属，并将发布链路升级为“双 ECS + 构建并行 + 切流串行”的稳定方案。
- 本 Stage 边界（不做）：不进入 S18 的真实微信支付/退款实现，不接入新的基础设施编排平台，不改造为 K8s/阿里云容器服务，不引入 ALB/RDS/WAF。

## 代码改动

- 主要改动文件：
  - `.github/workflows/deploy-backend.yml`
  - `docker-compose.backend.yml`
  - `docker-compose.web.yml`
  - `.env.prod.example`
  - `.env.prod.web.example`
  - `scripts/deploy_lib.sh`
  - `scripts/validate_prod_env.sh`
  - `scripts/deploy_backend.sh`
  - `scripts/deploy_admin_web.sh`
  - `scripts/bootstrap_prod.sh`
  - `scripts/deploy_prod.sh`
  - `scripts/reload_host_nginx.sh`
  - `scripts/start_backend_with_mvp_seed.sh`
  - `deploy/nginx/sunflower-host.conf.template`
  - `docs/CI-CD.md`
  - `docs/S19-Prod-Deployment-Config.md`
  - `docs/S14-Release-Runbook.md`
  - `docs/S14-Release-Acceptance-Checklist.md`
  - `docs/Backlog.md`
- 关键实现点：
  - 将生产发布拓扑拆成双 ECS：ECS-2 运行 `mysql + backend`，ECS-1 运行 `admin-web + host nginx`，ECS-1 通过内网 upstream 访问 ECS-2。
  - 新增 `docker-compose.backend.yml` 与 `docker-compose.web.yml`，保留原 `docker-compose.yml` 作为本地单机联调入口，不把本地开发链路一起打散。
  - 发布脚本增加 `DEPLOY_NODE_ROLE=backend|web` 角色感知；backend 节点只处理 backend/mysql，web 节点只处理 admin-web/nginx。
  - `reload_host_nginx.sh` 改为等待远端 backend upstream 健康，再刷新 ECS-1 宿主机 Nginx；admin 域名同时代理本机 `/` 和远端 `/api/`，避免后台浏览器额外 CORS 改造。
  - `deploy-backend.yml` 重构为 6 段链路：`detect-targets -> build-backend/build-admin-web(并行) -> prepare-backend-host/prepare-web-host(并行) -> deploy-backend-host -> deploy-web-host`。
  - `target=all` 与 `target=bootstrap` 现在固定执行 `ECS-2 backend -> ECS-1 admin-web -> ECS-1 nginx reload`，只改 backend 或只改 admin-web 时会自动跳过另一台主机。
  - `.env.prod` 模板拆成 backend/web 两份示例，避免把数据库、微信、短信等敏感配置混放到 web 节点。
  - 发布脚本的镜像拉取改为直接执行 `docker pull <image>`，避免 `docker compose pull <service>` 在 GHCR referrers 链路上偶发 `EOF` 导致 backend/web 节点误判发布失败。
  - 文档、Runbook、验收清单已同步更新到双 ECS 口径。

## 自动化测试

- 执行命令：`make stage-pre STAGE=S19`
- 结果：通过（提示 S19 已完成，但不影响 pre-check 通过）。
- 执行命令：`bash -n scripts/deploy_lib.sh scripts/validate_prod_env.sh scripts/deploy_backend.sh scripts/deploy_admin_web.sh scripts/bootstrap_prod.sh scripts/deploy_prod.sh scripts/reload_host_nginx.sh scripts/start_backend_with_mvp_seed.sh scripts/start_admin_web.sh`
- 结果：通过。
- 执行命令：`ruby -e 'require "yaml"; YAML.load_file(".github/workflows/deploy-backend.yml")'`
- 结果：通过。
- 执行命令：`bash -n scripts/deploy_lib.sh scripts/validate_prod_env.sh scripts/deploy_backend.sh scripts/deploy_admin_web.sh scripts/bootstrap_prod.sh scripts/deploy_prod.sh scripts/reload_host_nginx.sh scripts/start_backend_with_mvp_seed.sh scripts/start_admin_web.sh`
- 结果：通过。
- 执行命令：`docker compose -f docker-compose.backend.yml --env-file .env.prod.example config >/tmp/sunflower.backend.compose.yml`
- 结果：通过。
- 执行命令：`docker compose -f docker-compose.web.yml --env-file .env.prod.web.example config >/tmp/sunflower.web.compose.yml`
- 结果：通过。
- 执行命令：`PROD_ENV_FILE=.tmp.env.prod.backend.test bash scripts/validate_prod_env.sh`
- 结果：通过（backend 节点角色校验通过）。
- 执行命令：`PROD_ENV_FILE=.tmp.env.prod.web.test bash scripts/validate_prod_env.sh`
- 结果：通过（web 节点角色校验通过）。
- 执行命令：`python3 - <<'PY'
from pathlib import Path
workflow = Path(".github/workflows/deploy-backend.yml").read_text(encoding="utf-8")
assert "prepare-backend-host" in workflow
assert "prepare-web-host" in workflow
assert "deploy-backend-host" in workflow
assert "deploy-web-host" in workflow
assert "continue-on-error: true" in workflow
PY`
- 结果：通过（确认双主机 prepare/deploy job 已落地，bundle 上传仍保留 best-effort 容错）。
- 执行命令：`python3 - <<'PY'
from pathlib import Path
workflow = Path(".github/workflows/deploy-backend.yml").read_text(encoding="utf-8")
build_backend = workflow.index("build-backend:")
build_admin = workflow.index("build-admin-web:")
prepare_backend = workflow.index("prepare-backend-host:")
prepare_web = workflow.index("prepare-web-host:")
deploy_backend = workflow.index("deploy-backend-host:")
deploy_web = workflow.index("deploy-web-host:")
assert build_backend < prepare_backend < deploy_backend < deploy_web
assert build_admin < prepare_web < deploy_web
PY`
- 结果：通过（确认构建/上传可并行，正式切流保持 backend -> web 的串行顺序）。
- 执行命令：`python3 - <<'PY'
from pathlib import Path
template = Path("deploy/nginx/sunflower-host.conf.template").read_text(encoding="utf-8")
assert "__HOST_NGINX_ADMIN_SERVER_NAME__" in template
assert "__HOST_NGINX_API_SERVER_NAME__" in template
assert "__BACKEND_UPSTREAM_HOST__" in template
assert "__BACKEND_UPSTREAM_PORT__" in template
PY`
- 结果：通过。
- 执行命令：`cd sunflower-backend && mvn -B test`
- 结果：通过。
- 执行命令：`export PATH="$HOME/.local/node-v20.20.1-darwin-arm64/bin:$PATH" && cd sunflower-admin-web && npm run build`
- 结果：通过。
- 执行命令：`make stage-post STAGE=S19`
- 结果：通过。

## 人工复核步骤

1. 在 ECS-2 部署目录放置 backend 节点 `.env.prod`，至少填好 `DEPLOY_NODE_ROLE=backend`、MySQL、微信、短信、`BACKEND_HOST_PORT=8080`。
2. 在 ECS-1 部署目录放置 web 节点 `.env.prod`，至少填好 `DEPLOY_NODE_ROLE=web`、`BACKEND_UPSTREAM_HOST=172.25.121.83`、`BACKEND_UPSTREAM_PORT=8080`、`HOST_NGINX_ADMIN_SERVER_NAME`、`HOST_NGINX_API_SERVER_NAME`、证书路径。
3. 在 GitHub Actions 手动触发 `target=bootstrap`，确认 job 执行顺序为：`prepare-backend-host / prepare-web-host` 并行成功，随后 `deploy-backend-host` 成功，最后 `deploy-web-host` 成功。
4. 在 ECS-2 上执行 `curl http://127.0.0.1:8080/api/health`，确认 backend 健康；在 ECS-1 上执行 `curl http://172.25.121.83:8080/api/health`，确认内网 upstream 连通。
5. 访问 `https://<HOST_NGINX_API_SERVER_NAME>/api/health` 与 `https://<HOST_NGINX_ADMIN_SERVER_NAME>`，确认 API 和管理端都通过 ECS-1 对外可用。
6. 若本次仅改 backend，确认 workflow 不会触发 ECS-1 admin-web 重启；若本次仅改 admin-web，确认 workflow 不会触发 ECS-2 backend 发布。

## DoD Checklist

- [x] 范围单一（仅本 Stage）
- [x] 自动化测试已执行并通过
- [x] 人工复核步骤可复现
- [x] Backlog 状态已更新
- [x] `make stage-pre/post` 已通过

## API 契约影响

- 是否变更 API 契约：否
- 兼容说明：本次仅调整部署拓扑、workflow、脚本与运维文档，不修改后端接口路径、请求/响应结构与小程序调用协议。

## 风险与后续

- 当前已知风险：
  - 宿主机 HTTPS 证书仍需由运维先行签发并分别落到 ECS-1 的 `HOST_NGINX_ADMIN_TLS_CERT_PATH` / `HOST_NGINX_ADMIN_TLS_KEY_PATH` 与 `HOST_NGINX_API_TLS_CERT_PATH` / `HOST_NGINX_API_TLS_KEY_PATH`。
  - ECS-2 的 `8080` 必须通过安全组限制为仅允许 ECS-1 私网访问；否则会失去“备案入口统一在 ECS-1”的安全边界。
  - 当前仍使用自建 MySQL 容器；若后续流量增长，建议在 `S20` 之后评估 RDS/备份与监控增强。
- 下一 Stage 建议：
  - 进入 `S20`，围绕真实生产配置完成最终验收、对账、安全收口与上线 rehearsal。
