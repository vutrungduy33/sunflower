// Temporary bare-IP target for S19 deployment validation in WeChat DevTools.
// Real device or preview builds should switch back to a legal HTTPS domain.
const DEFAULT_API_BASE_URL = 'http://47.113.223.248';

module.exports = {
  DEFAULT_API_BASE_URL,
};
