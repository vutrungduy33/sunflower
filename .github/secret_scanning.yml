# Keep the WeChat DevTools project file excluded from GitHub secret scanning.
# The repository stores only the non-secret placeholder `touristappid` here, and
# PR gate `scripts/check_miniapp_project_config.sh` blocks real appids from being committed.
paths-ignore:
  - "sunflower-miniapp/project.config.json"
